Optional input lists.

Nothing here is required. The pipeline reads the parsed CSV folder and the
corpus folder directly; this folder exists so that any hand-maintained list
(files to exclude, a signer metadata table) has an obvious home next to the
code rather than in someone's home directory.
